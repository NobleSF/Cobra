#Session Model
  ip_address = session ip
  created_datetime = timestamp for session initialized

#For Login information
  username = account username, may be a phone number for artisans
  seller_pk = seller account primary key
  admin_pk = admin account primary key
  login datetime = timestamp for login event
    #if the user logs out, they will still have a login_datetime,
    #but no username or pk stored in the session.

#For checkout Information
  items_list = list of product_pk values
  shipping_address = key:value dictionary of shipping address
